# Skyrim Fitting System v1.7.1 SE-AE

## English

- Reduced redundant Papyrus script-type inspection and unused caller tracing on ordinary non-token Form keyword reads.
- Preserved first-call hook installation, late native binding, failed-install retries, and both SexLab P+ member observers.
- Limited inspection-history ownership to 128 entries with load/revert cleanup. Final-rendered nudity and strip/redress decisions are not cached or disabled.
- The 29 regression executables and source checks passed. In a controlled 256-lookup test, function-entry probes decreased from 3,096 to 24. This is not an in-game timing measurement.

**The reported NPC dialogue pause still needs in-game confirmation.** This update removes confirmed redundant work; it does not claim universal stall resolution. Only SFSCore.dll changes in the runtime ZIP. Keep settings, kits, saves, the helper ESL, and optional patches. Dependencies, supported game versions, save format, and API ABI are unchanged.

## 한국어

- Papyrus 스크립트 타입의 불필요한 반복 검사와 일반 아이템 키워드 조회의 미사용 호출자 추적을 줄였습니다.
- 최초 호출 훅, 늦은 native 등록, 설치 실패 재시도, SexLab P+의 두 멤버 관찰 경로를 유지했습니다.
- 검사 이력을 최대 128개로 제한하고 로드·리버트 시 정리합니다. 최종 렌더 상태의 나체 판정과 탈의·재착의 판단을 캐시하거나 끄지 않습니다.
- 회귀 검사 29개와 소스 검사가 통과했습니다. 동일 타입 256회 조회 분리 검사에서 함수 항목 검사가 3,096회에서 24회로 줄었습니다. 인게임 시간 측정값은 아닙니다.

**제보된 NPC 대화 멈춤 해소 여부는 인게임 확인이 필요합니다.** 확인된 중복 작업을 제거한 업데이트이며 모든 지연 해결을 보장하지 않습니다. 런타임 ZIP은 SFSCore.dll만 변경됩니다. 기존 설정·키트·세이브·보조 ESL·선택형 패치는 유지하세요. 선행 모드·지원 게임 버전·세이브 형식·API ABI는 변경하지 않았습니다.
