Fixed a v1.6.0 regression that could skip registered wig/clothing attachment when another plugin's custom-skin CALL hook was not recognized. Preserve the original game visitor and continue SFS attachments.
Fixed trampoline detection for longer ENDBR64-prefixed veneers and complete short veneers at readable page boundaries.
Retry partially available RaceMenu interfaces, early DAVE API negotiation and failed OAR registrations without discarding successful connections. Higher RaceMenu interface versions and Grid Costume messages use the last compatible public prefix with explicit assumption logs, not a version allowlist.
Restore saved dye on late registered attachments independently of BodyMorph availability. Failed DAVE API refresh now uses actor-local engine rebuild and retains dye/pose/heel follow-ups. Added initialization recovery, compatible-prefix and malformed-input tests; all 12 fast regression executables pass. Future ABI semantics and every historical RaceMenu ZIP are not individually verified.
Added executable production SE/AE hook regression tests. Existing verified IED filtering and actor-local refresh behavior are preserved.
Added hidden actual-equipment filtering for opaque CALL chains through the original engine visitor callbacks, preserving concrete object identity, vptr, RTTI and fields. Scoped filtering is isolated by thread/visitor and restores after nested calls and exceptions without changing inventory or worn flags.
Save/settings formats, prerequisites and optional compatibility-patch versions are unchanged. The original feedback environment and reporter-specific in-game result remain unverified. Foreign rendering outside the engine visitor interface is not covered by this callback fix.
